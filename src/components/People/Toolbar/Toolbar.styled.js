import styled from 'styled-components';

export const PeopleToolbarStyled = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;

  margin: -8px;
  & > * {
    padding: 8px;
  }
`;

export const PeopleToolbarItemsStyled = styled.div`
  display: flex;
  flex-wrap: wrap;

  margin: -8px;
  & > * {
    padding: 8px;
  }
`;
